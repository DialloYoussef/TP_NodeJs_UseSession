<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Formulaire de Connexion</title>
    <script src="/js/app.js" defer></script>

    <!-- Inclure Tailwind CSS (assurez-vous d'avoir configuré votre projet pour cela) -->
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <!-- Inclure Font Awesome -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    />
  </head>
  <body class="bg-gray-200">
    <header class="bg-blue-500 flex items-center justify-center mb-4">
      <div class="container mx-auto px-4 py-2 flex justify-between sm:flex-row">
        <a href="/" class="flex items-center lg:ml-2">
          <img src="img/logo.png" alt="Logo" class="w-20 h-20 lg:mr-4" />
          <span class="text-white font-bold text-2xl">Mon Site</span>
        </a>
        <nav class="flex items-center hidden md:flex">
          <ul class="flex items-center gap-4">
            <li>
              <a
                href="/add-user"
                class="text-white font-bold hover:text-blue-500"
              >
                <i class="fas fa-user-plus"></i> Ajouter un utilisateur
              </a>
            </li>
            <li>
              <a href="/users" class="text-white font-bold hover:text-blue-500">
                <i class="fas fa-users"></i> Liste des utilisateurs
              </a>
            </li>
            <li>
              <a href="/logout" class="text-white font-bold hover:text-red-300">
                <i class="fas fa-sign-out-alt"></i> Se déconnecter
              </a>
            </li>
          </ul>
        </nav>
        <button class="md:hidden toggleMenu">
          <i class="fas fa-bars"></i>
        </button>
      </div>
    </header>

